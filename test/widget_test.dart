// Ipon Buddy — placeholder test (Firebase requires a running emulator for full tests)
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
